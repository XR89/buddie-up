<% ratings.sample(5).each do |rating| %>
  <div class="rating-card">
    <div class="rating-card__info">
      <p>"<%= rating.comments %>"</p>
    </div>
    <div class="rating-card__rating">
      <% rating.rating.round.times do %>
        <span>⭐</span>
      <% end %>
    </div>
  </div>
<% end %>
